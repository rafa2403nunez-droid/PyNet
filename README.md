# PyNet
PyNet is an extension that embeds a Python .NET interpreter directly into Autodesk Navisworks, allowing users to execute Python scripts and develop custom automation workflows inside the application.ws Autodesk App Store guidelines, and is intended for professional use in production environments.
